# Python
__pycache__/
*.pyc
*.pyo
.venv*/
*.egg-info/

# Env
.env
.env.*
!.env.example
.corp-prod*
# GA4 secrets — never commit (previous inline-comment form here was invalid
# .gitignore syntax and silently matched nothing; verified before this fix)
ga4-service-account.json
ga4-oauth-client.json
ga4-oauth-token.json

# OS
.DS_Store
Thumbs.db

# Private infra docs / stray assets — keep OUT of this PUBLIC repo
box-multi-app-deploy.md
logo.png
docs/research/

# IDE / AI-tool config — personal to this machine, not project config
.claude/
.agents/
.codex/
.vscode/

# Data (generated by Actions)
data/

# Models / large binary
finstock/back/model/*.pkl
finstock/back/model/*.png
finstock/forecast_*.csv
finstock/model_pipeline.ipynb
finstock/run_forecast_*.py

# Visual brainstorming mockups (superpowers)
.superpowers/
