name: SBV Rates Crawl (Daily)

on:
  schedule:
    # Once daily: 9:30 AM VN (2:30 AM UTC)
    - cron: '30 2 * * *'
  workflow_dispatch:

jobs:
  crawl-sbv:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: pip install -r crawl_tools/requirements.txt

    - name: Run SBV crawler
      env:
        CRAWLING_BOT_DB: ${{ secrets.CRAWLING_BOT_DB }}
        HELPER_DB: ${{ secrets.HELPER_DB }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: |
        cd crawl_tools
        python crawl_sbv.py

    - name: Show results
      if: always()
      run: echo "SBV rates crawl completed at $(date)"
